# Drop independent --share payloads here, then run:
#   python scripts/evidence/replication_check.py replications/*.json
#
# See REPLICATION.md. Thresholds are preregistered and must not be adjusted.
